# SkillAlign
An end-to-end, skill-based job recommendation engine. It leverages the ESCO knowledge graph, transformer embeddings, and vector search to match users with ideal career paths through a clean API and interactive web interface.
